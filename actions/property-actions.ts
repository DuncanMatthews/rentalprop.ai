"use server";

export async function createProperty(formData: FormData): Promise<void> {
  // Store the result but don't return it

  console.log("Operation result:", formData);

  // Don't return anything
}
